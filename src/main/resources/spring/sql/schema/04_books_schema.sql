CREATE TABLE BOOKS (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) PRIMARY KEY,
	VERSION INTEGER NOT NULL,
	TITLE VARCHAR(100) NOT NULL,
	BOOK_FILE_ID INTEGER NOT NULL FOREIGN KEY REFERENCES BOOK_FILES(ID)
);
